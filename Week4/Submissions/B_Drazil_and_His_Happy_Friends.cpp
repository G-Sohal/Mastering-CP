#include <iostream>
#include <vector>
#include <algorithm>
#include <cmath>
#include <map>
#include <set>
#include <string>
#define ll long long
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    ll n, m; cin>>n>>m;
    ll b; cin>>b;
    vector<ll> x(b);
    for(ll i=0; i<b; i++) cin>>x[i];
    ll g; cin>>g;
    vector<ll> y(g);
    for(ll i=0; i<g; i++) cin>>y[i];
    
}